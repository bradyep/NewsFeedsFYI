import { Roles, ROLE_DB_NAMES } from './roles';
import { MINUTES_TO_CAHCE_FEED, MAX_NEWS_ITEMS, NUMBER_OF_COLUMNS } from './newsfeeds';

let roles = { Roles, ROLE_DB_NAMES };
let newsfeeds = { MINUTES_TO_CAHCE_FEED, MAX_NEWS_ITEMS, NUMBER_OF_COLUMNS };

export { roles, newsfeeds };
